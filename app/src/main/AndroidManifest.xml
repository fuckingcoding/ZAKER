<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mdzz.com.first_of_mdzz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.selectposition.SelectCityActivity"
            android:theme="@style/ActionBarTheme"
            ></activity>
        <activity android:name=".ui.web.WebActivity"></activity>
        <activity android:name=".ui.main.subscriber.SubItemBaseActivity"/>
        <activity android:name=".ui.selectposition.SelectPositionActivity" />
        <activity android:name=".ui.main.homeactivity.SportActivity"></activity>
    </application>

</manifest>